import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IFavoriteAlbum } from '@/shared/model/favorite-album.model';

const baseApiUrl = 'api/favorite-albums';

export default class FavoriteAlbumService {
  public find(id: number): Promise<IFavoriteAlbum> {
    return new Promise<IFavoriteAlbum>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IFavoriteAlbum): Promise<IFavoriteAlbum> {
    return new Promise<IFavoriteAlbum>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IFavoriteAlbum): Promise<IFavoriteAlbum> {
    return new Promise<IFavoriteAlbum>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IFavoriteAlbum): Promise<IFavoriteAlbum> {
    return new Promise<IFavoriteAlbum>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
